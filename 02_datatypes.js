"use strict"; // treat all JS code as newer version

// alert( 3 + 3) // we are using nodejs, not browser

console.log(3 
    +
     3) // code readability should be high so we don't do like this insted
console.log(3+3); //correct way
console.log("Vansh")


let name = "Vansh"
let age = 18
let isLoggedIn = false
let state;

// number => 2 to power 53
// bigint => use for large numbers
// string => "vansh"
// boolean => true/false
// null => standalone value
// undefined => when we don't assign any value to a varible then as output it shows undefined
// symbol => unique


// object => datatype

console.log(typeof undefined); // undefined
console.log(typeof null); //  datatype of null is object